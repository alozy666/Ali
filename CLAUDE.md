# CLAUDE.md — دليل المشروع لـ Claude Code

> **المشروع:** لعبة **«وَكُن مِنَ العارِفِينَ»** — لعبة مسابقات ومعارف إسلامية شيعية تفاعلية.
> **إعداد وتصميم:** محمد المسيح · by Mohamed Almseeh
> **الفرع المعتمد للتطوير:** `claude/islamic-knowledge-game-pp7miz` (ممنوع الدفع إلى `main`).
> **حالة الوثيقة:** v1.0 — القرارات الأساسية محسومة (انظر §12).

---

## 1. عن المشروع

لعبة مسابقات تُدار بين **لاعبَين أو أكثر / فريقَين أو أكثر**، بإدارة **حَكَم (Game Master)**، وتتكوّن من **أربع ألعاب**:

| # | اللعبة | جوهرها |
|---|--------|---------|
| 1 | **مَن يزيّد؟** | مزاد على عدد العناصر التي يستطيع الفريق تعدادها ضمن تصنيف معيّن |
| 2 | **رحلة السفر** | مسار من 7 محطات: مكة → المدينة → النجف → كربلاء → الكاظمية → مشهد → سامراء |
| 3 | **لَمِّح إليّ** | 3 كلمات مفتاحية ← استنتاج الشخصية/الكتاب/الحدث/المفهوم |
| 4 | **اسأل وجاوب** | أسئلة مباشرة: عقائد، فقه، قرآن، تاريخ |

**أنماط الجلسة:** (1) رحلة السفر الكاملة — (2) لعبة منفردة — (3) النمط الشامل (جولات متنوعة عشوائياً).

---

## 2. ⚠️ القاعدة الذهبية للمخرجات (Global Output Rule)

**كل** رسالة أو شاشة أو جولة أو سؤال يُعرض على اللاعبين — في الدردشة أو في الواجهة أو في أي مستند يراه المستخدم النهائي — يجب أن ينتهي بالتوقيع التالي حرفياً:

```
by Mohamed Almseeh
إعداد: محمد المسيح
```

في الواجهة: يظهر التوقيع ثابتاً في تذييل كل شاشة (`<footer class="signature">`). لا يجوز حذفه أو تصغيره لدرجة عدم القراءة.

---

## 3. المخرجات (Tracks)

**القرار المعتمد:** المساران يُبنيان **بالتوازي**، ويتشاركان **نفس بنك الأسئلة ونفس القوانين**. البنك هو مصدر الحقيقة الوحيد.

### المسار A — حزمة الحَكَم الآلي (`prompts/`)
ملفات نصية جاهزة لتشغيل اللعبة داخل Claude مباشرةً بلا برمجة:
- `prompts/game-master.md` — برومبت الحَكَم الكامل (القوانين + الأوقات + الكروت + التوقيع).
- `prompts/logo-prompt.md` — برومبت توليد الشعار 3D لمولّدات الصور.
- `prompts/question-style-guide.md` — قواعد صياغة الأسئلة ومنع التكرار.

### المسار B — تطبيق الويب: **ملف HTML واحد قائم بذاته**
المخرج النهائي ملف واحد `dist/wakun-min-al-arifeen.html` يُحمَّل ويُفتح مباشرةً بالنقر المزدوج — **بلا خادم وبلا إنترنت وبلا تثبيت**. يُدار على **جهاز واحد يشغّله الحَكَم** والجميع يرى الشاشة.

> ⚠️ **أهم قيد تقني في المشروع:** عند الفتح عبر `file://` تمنع المتصفحات دالة `fetch()` من قراءة ملفات JSON المجاورة. لذلك:
> - نؤلّف المصادر **مُجزّأة** في `data/*.json` و`web/js/**` (للصيانة والمراجعة).
> - ويقوم `tools/build.js` بـ**دمج كل شيء** (بيانات + كود + أنماط + خطوط/صور بصيغة base64) داخل ملف HTML واحد.
> - **ممنوع** أي `fetch` أو `import` من ملف خارجي في المخرج النهائي، وممنوع أي رابط CDN.
> - `localStorage` قد يكون معطّلاً على `file://` في بعض المتصفحات ⟵ كل قراءة/كتابة داخل `try/catch` مع سقوط آمن إلى الذاكرة، مع زرّ **«تصدير/استيراد الجلسة»** كبديل موثوق.

---

## 4. بنية المستودع (المخطّطة)

```
/CLAUDE.md                 ← هذا الملف
/milestones.md             ← خارطة الطريق والمراحل
/README.md                 ← تعريف اللعبة وطريقة التشغيل
/prompts/                  ← المسار A: حزمة الحَكَم الآلي
/data/
   schema.json             ← مخطط السؤال (JSON Schema)
   sources.md              ← قائمة المصادر المعتمدة ورموزها
   bidding/                ← تصنيفات «مَن يزيّد؟»
   journey/                ← أسئلة المحطات السبع (ملف لكل محطة)
   hints/                  ← بطاقات «لَمِّح إليّ»
   qa/                     ← أسئلة «اسأل وجاوب»
/web/
   index.html
   css/                    ← الهوية البصرية
   js/
     engine/               ← الحالة، المؤقّت، النقاط، الكروت، منع التكرار
     games/                ← bidding.js · journey.js · hints.js · qa.js
     ui/                   ← الشاشات والمكوّنات
/tools/
   validate.js             ← صحة JSON · تفرّد المعرّفات · وجود المصادر · كشف التكرار
   build.js                ← يدمج كل شيء في ملف HTML واحد
/dist/
   wakun-min-al-arifeen.html   ← 🎯 المخرج النهائي القابل للتحميل والتوزيع
/legacy/                   ← الملفات القديمة (patterns-game) — غير ذات صلة
```

---

## 5. القوانين التفصيلية (المرجع التنفيذي)

### 5.1 مَن يزيّد؟
1. الحَكَم يطرح تصنيفاً (مثال: ألقاب الزهراء ع / أصحاب الإمام الحسين ع / مصنّفات الشيخ الطوسي).
2. الفرق تتزايد على **عدد** العناصر التي تستطيع سردها.
3. صاحب أعلى مزايدة يلتزم بالتعداد خلال **30 ثانية**.
4. نجح كاملاً ⟵ نقاط الجولة له. فشل أو انتهى الوقت ⟵ النقاط **تلقائياً** للمنافس.
5. كل تصنيف في البنك يحمل `min_valid` (الحد الأدنى المقبول) و`items[]` (العناصر الصحيحة مع مصادرها) و`accept_aliases` (صيغ بديلة مقبولة للاسم).

### 5.2 رحلة السفر (7 محطات)
| # | المحطة | المحور |
|---|--------|--------|
| 1 | مكة المكرمة | السيرة النبوية المبكرة، المبعث، الدعوة |
| 2 | المدينة المنورة | النبي (ص)، الزهراء، الحسن المجتبى، أئمة البقيع (ع) |
| 3 | النجف الأشرف | سيرة وعلم أمير المؤمنين (ع) |
| 4 | كربلاء المقدسة | النهضة الحسينية، الإمام الحسين وأصحابه (ع) |
| 5 | الكاظمية المقدسة | الإمام الكاظم والإمام الجواد (ع) |
| 6 | مشهد المقدسة | الإمام الرضا (ع) |
| 7 | سامراء المقدسة | الإمام الهادي والإمام العسكري (ع) |

- **3 أسئلة لكل فريق في كل محطة**: سهل + متوسط + صعب.
- نمط السؤال عشوائي: **خيارات متعددة** أو **صح/خطأ**.
- **60 ثانية** لكل سؤال.
- **الكروت (3 لكل فريق، مرة واحدة طوال الرحلة):**
  - `pass` **[خليها لغيري]** — تحويل السؤال للمنافس: أجاب صح ⟵ النقاط له، أجاب خطأ ⟵ النقاط للمحوِّل.
  - `swap` **[تغيير السؤال]** — سؤال بديل **بنفس درجة الصعوبة**.
  - `options` **[خيارات للسؤال]** — إضافة خيارات لسؤال ظهر بلا خيارات.
- **مع ثلاثة فرق فأكثر:** الفريق الذي يلعب كرت [خليها لغيري] **يختار** الفريق المُحال إليه.
- **التعادل في نهاية الجلسة:** سؤال «موت مفاجئ» من فئة «صعب»، ويتكرر حتى الحسم.

### 5.3 لَمِّح إليّ
- 3 كلمات مفتاحية ← إجابة واحدة. مثال: (شقشقية · بلاغة · المتقين) ⟵ **نهج البلاغة**.
- الأسرع في الإجابة الصحيحة يحصد نقاط الجولة.
- كل بطاقة تحمل `answer` + `accept[]` (صيغ مقبولة) + تلميح رابع اختياري `extra_hint`.

### 5.4 اسأل وجاوب
- أسئلة مباشرة، **60 ثانية** للإجابة.
- **الكروت (3 لكل فريق، مرة واحدة):**
  - `options` **[خيارات للسؤال]** — تحويل السؤال إلى خيارات متعددة.
  - `ask_help` **[اسأل الجمهور أو صديق]** — مهلة إضافية أو تلميح مباشر من الحَكَم.
  - `fifty` **[حذف إجابتين]** — إزالة خيارين خاطئين (50:50)، يتطلب وجود خيارات.

### 5.5 جدول الأوقات والنقاط (افتراضيات قابلة للضبط)
| البند | القيمة الافتراضية |
|------|--------------------|
| مؤقّت التعداد (مَن يزيّد؟) | 30 ث |
| مؤقّت السؤال (رحلة السفر / اسأل وجاوب) | 60 ث |
| مؤقّت التخمين (لَمِّح إليّ) | 45 ث |
| سؤال سهل / متوسط / صعب | 10 / 20 / 30 نقطة |
| مَن يزيّد؟ (نجاح كامل) | 25 نقطة + 2 لكل عنصر فوق المزايدة |
| لَمِّح إليّ | 20 نقطة (تنقص 5 عند كشف تلميح إضافي) |
| إكمال محطة بكل أسئلتها صحيحة | مكافأة 15 نقطة |

> **معتمدة كقيم افتراضية سارية** ما لم يطلب صاحب المشروع تعديلها. تُقرأ كلها من `data/config.json` ولا تُكتب داخل الكود مباشرةً.

---

## 6. نموذج بيانات السؤال

```json
{
  "id": "journey-karbala-hard-014",
  "game": "journey",
  "station": "karbala",
  "difficulty": "hard",
  "type": "mcq",
  "text": "مَن حمل راية الإمام الحسين (ع) يوم عاشوراء؟",
  "options": ["حبيب بن مظاهر", "أبو الفضل العباس (ع)", "زهير بن القين", "مسلم بن عوسجة"],
  "answer": 1,
  "accept": ["العباس", "أبو الفضل العباس", "قمر بني هاشم"],
  "explanation": "شرح موجز يُعرض بعد الإجابة.",
  "source": { "book": "الإرشاد", "author": "الشيخ المفيد", "ref": "ج2" },
  "tags": ["عاشوراء", "أصحاب الحسين"]
}
```

**حقول إلزامية:** `id`, `game`, `difficulty`, `type`, `text`, `answer`, `source`.
**أنواع `type` المسموحة:** `mcq` · `tf` · `open` · `list` (لمَن يزيّد؟) · `hint` (لَمِّح إليّ).

---

## 7. سياسة المحتوى والدقة (غير قابلة للتفاوض)

1. **المصادر المعتمدة:** الكافي (الكليني) · من لا يحضره الفقيه (الصدوق) · تهذيب الأحكام والاستبصار (الطوسي) · بحار الأنوار (المجلسي) · الإرشاد (المفيد) · إعلام الورى (الطبرسي) · نهج البلاغة · الصحيفة السجادية · القرآن الكريم.
2. **كل سؤال يحمل مصدره** في حقل `source`. سؤال بلا مصدر لا يدخل البنك.
3. **لا تختلق مصدراً ولا رقم صفحة.** عند عدم التيقّن من موضع دقيق: اذكر الكتاب والمؤلف فقط، أو استبعد السؤال.
4. **المسائل الخلافية:** تُصاغ بصيغة «بحسب المشهور عند الإمامية…» ولا تُطرح كسؤال إجابته قاطعة إن لم تكن كذلك.
5. **مسائل الفقه العملي** (أحكام تكليفية) تُقدَّم كمعرفة عامة مع تنبيه: «للعمل يُرجع إلى فتوى المرجع».
6. **الأدب في الخطاب:** اللعبة معرفية تعليمية. يُمنع أي محتوى يسبّ أو يستهزئ بأي شخصية أو مذهب أو طائفة. التركيز على سيرة أهل البيت (ع) وعلومهم لا على السجال.
7. **الألقاب:** (ص) للنبي وآله، (ع) للمعصومين، (رض/رحمه الله) لمن دون ذلك — بشكل متّسق في كل النصوص.
8. **الأرقام والتواريخ** تُذكر بالهجري أولاً ثم الميلادي عند الحاجة.

---

## 8. معايير الواجهة والهوية البصرية

- **اللغة والاتجاه:** عربي، `dir="rtl"`, `lang="ar"` على مستوى المستند.
- **لوحة الألوان:** أخضر زمردي عميق `#0B3D2E` · كحلي غني `#0A1A3C` · ذهب متوهّج `#D4AF37` · عاجي `#F5EFE0`.
- **الشعار:** طابع 3D سينمائي — طريق من نور يمتد من الكعبة إلى القباب الذهبية في سامراء، يحيط به كتاب مضيء وإسطرلاب ذهبي. برومبت التوليد محفوظ في `prompts/logo-prompt.md`.
- **الخطوط:** خط عربي عريض للعناوين + خط واضح للنصوص، مع تدرّج احتياطي آمن (`system-ui`, `Tahoma`) حتى تعمل الواجهة بلا إنترنت.
- **الوضع الليلي والنهاري** مدعومان، والتصميم يعمل على عرض 400px (هاتف) دون تمرير أفقي.
- **الحركة:** انتقالات هادئة وعدّاد تنازلي واضح؛ يُحترم `prefers-reduced-motion`.
- **التذييل:** التوقيع في §2 ثابت في كل شاشة.

---

## 9. قواعد تقنية

- **بلا اعتماديات خارجية:** HTML + CSS + JavaScript خالص. لا أُطر عمل ولا npm install لتشغيل اللعبة.
- **خطوة بناء واحدة فقط** (`node tools/build.js`) مهمّتها الدمج في ملف واحد — وليست ترجمةً أو حزماً.
- **بلا شبكة إطلاقاً** أثناء اللعب: كل الأصول مضمّنة داخل الملف.
- **بلا توليد لحظي للأسئلة:** البنك ثابت ومراجَع بالكامل (قرار §12/2)، والمحرّك لا يستدعي أي ذكاء اصطناعي.
- **فصل تام:** المحرّك (`engine/`) لا يعرف شيئاً عن نصوص الأسئلة؛ الألعاب (`games/`) لا تلمس الـ DOM مباشرةً إلا عبر `ui/`.
- **منع التكرار:** `engine/dedupe.js` يحتفظ بسجلّ الأسئلة المستهلكة في `localStorage` ولا يعيد سؤالاً حتى استنفاد فئته.
- **العشوائية قابلة للبذر (seeded)** ليتمكن الحَكَم من إعادة إنتاج جلسة.
- **التطوير:** `python3 -m http.server 8000` ثم `http://localhost:8000/web/` — **الاختبار النهائي إلزامي عبر فتح `dist/…html` بـ`file://` مباشرةً.**
- **التحقّق قبل كل دفع:** `node tools/validate.js` (صحة JSON + تفرّد المعرّفات + وجود المصادر + كشف التكرار).

---

## 10. سير العمل مع Git

- التطوير على `claude/islamic-knowledge-game-pp7miz` حصراً. **لا دفع إلى `main` ولا فتح PR إلا بطلب صريح.**
- الدفع دائماً: `git push -u origin claude/islamic-knowledge-game-pp7miz`.
- رسائل الـ commit وصفية بالعربية أو الإنجليزية، تبدأ بنطاق: `docs:` `engine:` `data:` `ui:` `prompts:`.
- ملفات بنك الأسئلة تُضاف بدفعات صغيرة مع ذكر عدد الأسئلة المضافة في الرسالة.

---

## 11. طريقة إدارة الجلسة (للحَكَم الآلي)

1. الترحيب + وصف الشعار المضيء (الطريق من مكة إلى سامراء) لإدخال اللاعبين في الأجواء.
2. تسجيل أسماء اللاعبين/الفرق.
3. عرض الخيارات: **[1]** رحلة السفر الكاملة · **[2]** لعبة محدّدة · **[3]** النمط الشامل.
4. بعد **كل** جولة: عرض **جدول النقاط** + **رصيد الكروت المتبقية** لكل فريق.
5. الالتزام بالمؤقّتات وإعلانها، وبالتوقيع في نهاية كل رسالة.

---

## 12. القرارات المفتوحة (تُحسم مع صاحب المشروع)

| # | القرار | الحسم |
|---|--------|-------|
| 1 | المخرجات | ✅ **المساران معاً بالتوازي**: حزمة الحَكَم + تطبيق الويب |
| 2 | بنك الأسئلة | ✅ **ثابت بالكامل** — كل سؤال مكتوب ومراجَع بمصدره، لا توليد لحظي |
| 3 | نمط الأجهزة | ✅ **جهاز واحد يديره الحَكَم**، والجميع يرى الشاشة |
| 4 | النشر | ✅ **ملف HTML واحد** يُحمَّل ويُفتح محلياً (`dist/`) |
| 5 | جدول النقاط §5.5 | ✅ معتمد كافتراضي ساري، قابل للتعديل لاحقاً عبر `config.json` |
| 6 | [خليها لغيري] مع 3 فرق+ | ✅ المحوِّل يختار الفريق المُحال إليه |
| 7 | التعادل | ✅ سؤال «موت مفاجئ» من فئة صعب |

**نتائج مباشرة على التنفيذ:** بنك ثابت ⟵ حجم البنك هو عنق الزجاجة الحقيقي (M2 و M7 أولوية). ملف واحد ⟵ لا `fetch` ولا CDN، والبناء عبر `tools/build.js`. جهاز الحَكَم ⟵ لا حاجة لخادم ولا مزامنة، والواجهة تُصمَّم للعرض من مسافة (خطوط كبيرة وتباين عالٍ).

---

## 13. مصادر التصميم المثبّتة (`.claude/skills/`)

مثبَّتة في المستودع نفسه، فتعمل في كل جلسة لاحقة على هذا المشروع.

### المصدران
| المصدر | السكلات | الاستعمال |
|--------|---------|-----------|
| `nextlevelbuilder/ui-ux-pro-max-skill` | `ui-ux-pro-max` · `design` · `design-system` · `brand` · `banner-design` · `slides` | ذكاء تصميم UI/UX: أنماط، لوحات ألوان، اقترانات خطوط، إرشادات تجربة، أيقونات |
| `freshtechbro/claudedesignskills` | 22 سكلاً للرسوم ثلاثية الأبعاد والحركة | ثلاثي الأبعاد، الحركة، التمرير، الانتقالات |

**أداة البحث تعمل** (مُختبَرة):
```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "<query>" --domain style|color|typography|ux|icons|gsap|chart
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "<product> <keywords>" --design-system
```

### ⚠️ القيد الحاكم: الملف الواحد يمنع الـCDN

المخرج النهائي ملف HTML واحد يُفتح عبر `file://` (§3). أي مكتبة تقترحها هذه السكلات **يجب أن تُضمَّن داخل الملف**، لا أن تُحمَّل من رابط. فالحكم على أي اقتراح هو **كلفته بالكيلوبايت**:

| المكتبة | الحجم المصغّر (تقريبي) | الحكم لمشروعنا |
|---------|------------------------|-----------------|
| **CSS 3D خالص** (`transform: perspective/rotate3d`) | **0 ك.ب** | ✅ **الخيار الأول** — كروت تنقلب، شارات ذهبية، عمق المحطات |
| **Vanilla-Tilt** | ~5 ك.ب | ✅ ميلان الكروت مع الماوس |
| **anime.js** | ~17 ك.ب | ✅ المؤقّت، انتقال المحطات، احتفال الفوز |
| **Zdog** | ~28 ك.ب | ✅ **شعار 3D متجهي** يُرسم بالكود — قبّة ومسار نور، بلا صورة |
| **GSAP core** | ~70 ك.ب | 🟡 قوي لكن anime.js يكفي هنا |
| **three.js** | ~600 ك.ب | 🟡 يرفع الملف من 141 ك.ب إلى ~750 ك.ب — فقط لو أردت مشهد 3D حقيقياً |
| **Vanta.js** | يتطلب three.js | 🟡 تابع لما سبق |
| **PixiJS / Babylon.js / PlayCanvas** | 400 ك.ب – 4 م.ب | ❌ مبالغة لواجهة مسابقات |
| **React** (`react-three-fiber` · `motion-framer` · `react-spring`) | — | ❌ المشروع JavaScript خالص بلا أُطر (§9) |

**الخلاصة العملية:** الهدف البصري (شعار ثلاثي الأبعاد، عمق، حركة فخمة) يتحقّق بـ **CSS 3D + Zdog + anime.js** بكلفة **~45 ك.ب** إجمالاً. `three.js` يُطرح على صاحب المشروع كقرار منفصل لأنه يضاعف حجم الملف خمس مرات.

### قواعد الحوكمة
1. مخرجات هذه السكلات **توصيات**، لا أوامر. قواعد `CLAUDE.md` وقرارات صاحب المشروع تعلو عليها دائماً.
2. لا تُستدعى سكربتات هذه السكلات إلا صراحةً، ولا تُمرَّر إليها بيانات المشروع الخاصة.
3. سكربتات توليد الشعارات فيها تعتمد على مفاتيح API خارجية غير متوفّرة — برومبت الشعار في `prompts/logo-prompt.md` هو المسار المعتمد.
4. عند تعارض توصية تصميمية مع **إمكانية القراءة من مسافة** أو مع **دعم RTL** ⟵ تُرفض التوصية.

---

by Mohamed Almseeh
إعداد: محمد المسيح
