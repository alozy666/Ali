<?php

declare(strict_types=1);

/**
 * نموذج الإعدادات.
 *
 * انسخه إلى `config.php` بنفس المجلد وعبّئ القيم. الملف `config.php` مستثنى من
 * git (راجع .gitignore) ولا يُرفع أبداً.
 *
 * لماذا ملف PHP بدل .env: على الاستضافة المشتركة لا نضمن توفّر Composer ولا
 * حزمة dotenv، وملف PHP داخل `app/` (خارج public_html) لا يُقرأ من الويب أصلاً.
 */

return [
    // ── قاعدة البيانات ────────────────────────────────────────────────────
    'db' => [
        'host'     => 'localhost',
        'port'     => 3306,
        'name'     => 'u000000000_lifeos',
        'user'     => 'u000000000_lifeos',
        'pass'     => 'ضع-كلمة-مرور-قاعدة-البيانات',
        'charset'  => 'utf8mb4',
    ],

    // ── مزوّد النماذج (قسم ٦ و٣٦) ─────────────────────────────────────────
    'llm' => [
        'api_key' => 'sk-ant-...',

        // توزيع النماذج حسب المهمة — شرط إلزامي بقسم ٣٦، لا تحسين اختياري.
        // استخدام نموذج قوي لكل شيء يحرق ميزانية ٢٥-٣٥$ خلال أيام.
        'models' => [
            // ~٩٠٪ من عمل النظام: استخراج الكيانات، التصنيف، الفرز
            'light'    => 'claude-haiku-4-5',
            // الردود والمحادثة العادية
            'standard' => 'claude-sonnet-5',
            // القرارات المركّبة والتحليل المالي — نادر يومياً
            'deep'     => 'claude-opus-5',
        ],

        // السقف الصارم (قسم ٣٦): عند تجاوزه يخفّ التشغيل الاستباقي تلقائياً.
        'monthly_budget_usd' => 25.0,
    ],

    // ── تحويل الصوت لنص (قسم ٨) ───────────────────────────────────────────
    'stt' => [
        'api_key' => 'ضع-مفتاح-Deepgram',
        'model'   => 'nova-3',
        // اللهجة الخليجية مع مصطلحات إنجليزية متداخلة (قسم ٣٢)
        'language' => 'multi',
    ],

    // ── إشعارات الويب (قسم ١٢ و٢٢) ────────────────────────────────────────
    'push' => [
        'vapid_subject'     => 'mailto:you@example.com',
        'vapid_public_key'  => '',
        'vapid_private_key' => '',
        // ضابط قسم ٢٢: حد أقصى للتنبيهات باليوم مهما «استحق» الإرسال نظرياً
        'max_per_day'       => 6,
    ],

    // ── الأمان (قسم ٩) ────────────────────────────────────────────────────
    'security' => [
        // مفتاح تشفير الحقول الحساسة (مالية/نفسية) — 32 بايت بـ base64.
        // ولّده مرة واحدة بـ: php -r "echo base64_encode(random_bytes(32));"
        // لو ضاع هذا المفتاح، البيانات المشفّرة لا تُسترجع. احفظ نسخة منه بمكان آمن.
        'field_key_base64' => '',

        // رمز مصادقة مبدئي للواجهة البرمجية.
        // ⚠ غير كافٍ للإنتاج: قسم ٩ يشترط 2FA على كل نقطة دخول وانتهاء صلاحية
        // للجلسة. استبدله بمصادقة حقيقية قبل إدخال أي بيانات فعلية.
        // ولّده بـ: php -r "echo bin2hex(random_bytes(32));"
        'api_token' => '',
    ],

    // ── التشغيل ───────────────────────────────────────────────────────────
    'runtime' => [
        'timezone' => 'Asia/Bahrain',
        // حجم دفعة الطابور بكل تشغيل cron. اضبطه أقل من max_execution_time
        // بخطتك مقسوماً على متوسط زمن المهمة. راجع docs/01-architecture.md.
        'queue_batch_size' => 5,
        // سقف زمني لتشغيل العامل بالثواني — يوقف الدفعة بأمان قبل أن يقتله السيرفر
        'worker_time_budget_seconds' => 45,
        'storage_path' => __DIR__ . '/../storage',
    ],
];
